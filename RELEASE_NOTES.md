# Release Notes

## v1.1.1 (2026-03-21)

### 修正（Export / Import）
- Windows でのエクスポート時に共有メニューではなく保存先選択ダイアログを表示し、任意フォルダへ保存するよう修正。
- カテゴリの Export / Import に対応し、カテゴリとエントリの紐付きを復元。
- ZIP / JSON インポート時に同名エントリは新規作成せず更新（upsert）するよう変更し、重複登録を防止。
- 別DBへ ZIP をインポートした際に 0 件になる問題を修正（ID ではなく対象DB内の名称基準で判定）。
- ZIP インポート時の画像（サムネイル）復元処理を見直し、取り込み漏れを防止。
- JSON インポートの読み込み経路を改善し、巨大ファイル時のクラッシュを回避しやすい実装へ調整（ネイティブではファイルパス経由読込、サイズガード追加）。

### 修正（検索）
- 検索条件が未入力の初期状態で全件表示されるよう修正。
- 条件入力時のみ、テキスト・タグ・QR登録状態の絞り込みを適用。

### テスト
- 追加テスト:
  - `test/data/repositories/export_repository_test.dart`
  - `test/data/repositories/qr_repository_search_test.dart`

## v1.1.0 (2026-03-21)

### 追加・改善（アプリ本体）
- スキャン保存画面で、サムネイルに加えてタグの選択・新規作成・同時保存に対応。
- スキャン時の保存情報編集で、データ形式（テキスト/バイナリ）の自動判定を維持しつつ、手動設定も可能。
- QR未登録エントリのサムネイルを、カード表示に加えて詳細画面でもグレースケール表示。

### 修正（Export / Import）
- JSONインポート時の文字化けを修正（UTF-8デコードを明示）。
- タグがデフォルトDBに紐づいてしまう問題を修正し、対象DBへ正しくインポート。
- タグIDの再生成に伴う紐付け不整合を、旧ID→新IDマッピングで復元。

### 追加・改善（DBエディタ）
- 右ペインをタブ化し、`エントリ詳細` / `分類編集` を切替可能に。
- `分類編集` タブでカテゴリ・タグの追加/改名/削除を実施可能。
- 一覧テーブルで `お気に入り` / `テキストモード` のON/OFFを直接操作可能。
- 一覧テーブルから既存エントリ削除（確認ダイアログ付き）に対応。
- 雛形生成時（Excel/ODS）に `data` / `thumbnails` フォルダ作成を行わないよう修正し、macOSでの保存時Permissionエラーを回避。

### テスト・品質
- 追加テスト:
  - `test/core/utils/qr_data_type_utils_test.dart`
  - `test/data/repositories/export_repository_test.dart`
  - `tools/db_editor/test/editor_state_notifier_test.dart`
- 既存関連テストを再実行し、`flutter analyze` も含めて問題なしを確認。
